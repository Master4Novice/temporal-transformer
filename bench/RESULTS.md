# Benchmark Results

> Generated by `npm run bench`. Numbers reflect a single run on the listed environment — your mileage will vary.

**Environment:** Node v24.16.0, darwin arm64
**Date:** 2026-05-26T13:56:59.675Z
**Methodology:** 500ms warmup, 2000ms measurement window, 200 calls per sample using `process.hrtime.bigint()`. Reported as mean of all samples; ops/sec derived from mean.

Run yourself with:

```bash
git clone https://github.com/Master4Novice/temporal-transformer.git
cd temporal-transformer
npm install && npm run bench
```

## convert epoch → formatted date

Default-format conversion. Library uses dual-TZ output (local + GMT) plus relative-time computation; baselines do single-TZ ISO output only.

| Implementation | ops/sec | mean | median | p99 | vs fastest |
|---|---:|---:|---:|---:|---:|
| `new Date(ms).toISOString()` | 2.08M ops/s | 482 ns | 456 ns | 1.26 µs | — (fastest) |
| `DateTime.fromMillis(ms).toFormat(...)` | 527K ops/s | 1.90 µs | 1.85 µs | 2.20 µs | 3.9× slower |
| **convertEpoch(ms)** | 25K ops/s | 39.44 µs | 39.11 µs | 47.36 µs | 81.9× slower |

## convert epoch → formatted date in specific timezone

Single-TZ formatted output. The wrapper is much thinner here.

| Implementation | ops/sec | mean | median | p99 | vs fastest |
|---|---:|---:|---:|---:|---:|
| `DateTime.fromMillis(ms,{zone}).toFormat(...)` | 183K ops/s | 5.46 µs | 5.42 µs | 5.97 µs | — (fastest) |
| **convertEpochToTimezone(ms, tz)** | 168K ops/s | 5.94 µs | 5.86 µs | 6.66 µs | 1.1× slower |

## parse ISO 8601 string → epoch

Strict ISO parsing. Library adds input-allowlist + length-cap checks.

| Implementation | ops/sec | mean | median | p99 | vs fastest |
|---|---:|---:|---:|---:|---:|
| `new Date(iso).getTime()` | 7.76M ops/s | 129 ns | 127 ns | 139 ns | — (fastest) |
| `DateTime.fromISO(iso).toMillis()` | 603K ops/s | 1.66 µs | 1.62 µs | 2.11 µs | 12.9× slower |
| **parseToEpoch(iso, undefined, 'UTC')** | 219K ops/s | 4.56 µs | 4.41 µs | 6.14 µs | 35.4× slower |

## calendar-accurate duration between two epochs

Raw arithmetic is fastest but gives only ms. Luxon computes years/months/days. Library wraps + freezes + builds humanReadable string.

| Implementation | ops/sec | mean | median | p99 | vs fastest |
|---|---:|---:|---:|---:|---:|
| `to - from (ms diff only)` | 226.76M ops/s | 4 ns | 4 ns | 5 ns | — (fastest) |
| `Luxon DateTime.diff(units)` | 87K ops/s | 11.46 µs | 11.32 µs | 12.85 µs | 2598.7× slower |
| **getDurationBetween(from, to)** | 92K ops/s | 10.90 µs | 10.71 µs | 12.55 µs | 2472.0× slower |

## auto-detect epoch unit (the unique feature)

No direct baseline — auto-detection is what this library uniquely does.

| Implementation | ops/sec | mean | median | p99 | vs fastest |
|---|---:|---:|---:|---:|---:|
| **getEpochUnit(ms)** | 170.14M ops/s | 6 ns | 6 ns | 6 ns | 1.0× slower |
| **getEpochUnit(s) [seconds-scale input]** | 177.73M ops/s | 6 ns | 6 ns | 6 ns | — (fastest) |
| **getEpochUnit('1622547800000') [string input]** | 80.48M ops/s | 12 ns | 12 ns | 15 ns | 2.2× slower |

## timezone offset lookup

Cheap operation — measures the validTimezone + Luxon offset call.

| Implementation | ops/sec | mean | median | p99 | vs fastest |
|---|---:|---:|---:|---:|---:|
| `DateTime.now().setZone(tz).offset` | 241K ops/s | 4.15 µs | 4.07 µs | 5.21 µs | — (fastest) |
| **getTimezoneOffset(tz)** | 131K ops/s | 7.61 µs | 7.53 µs | 8.54 µs | 1.8× slower |

## safe predicate isValidEpoch

No-throw boolean check. Trivial JS alternative misses the auto-detect range check.

| Implementation | ops/sec | mean | median | p99 | vs fastest |
|---|---:|---:|---:|---:|---:|
| `typeof === 'number' && isFinite && !isNaN` | 227.47M ops/s | 4 ns | 4 ns | 5 ns | — (fastest) |
| **isValidEpoch(ms)** | 166.95M ops/s | 6 ns | 6 ns | 6 ns | 1.4× slower |

## Result-style API on error path

safeConvertEpoch wraps a try/catch around the throwing variant. Cost of the error branch.

| Implementation | ops/sec | mean | median | p99 | vs fastest |
|---|---:|---:|---:|---:|---:|
| **safeConvertEpoch(null) [error branch]** | 255K ops/s | 3.92 µs | 3.85 µs | 5.08 µs | — (fastest) |
| **safeConvertEpoch(ms) [success branch]** | 24K ops/s | 41.44 µs | 36.78 µs | 118.46 µs | 10.6× slower |

## What this means in practice

| Use case | Verdict |
|---|---|
| **Backend ingest / API endpoints** — 100s to 10K timestamps per second | Performance is irrelevant; the library is plenty fast. |
| **Per-request formatting** — a handful of calls per HTTP request | Per-call cost is negligible against network/DB latency. |
| **Batch processing** — 100K+ timestamps per second | Costs become visible. Prefer raw Luxon or native `Date` in the hot path; use the library at the boundary for validation/auto-detect. |
| **Frontend display** — a few timestamps in a UI | Imperceptible. |
| **CSV/JSON exports** of millions of rows | Use the auto-detect once to determine the unit, then process in a tight raw-`Date` loop. |

The library is a *validation + ergonomics layer*, not a hot-path date primitive. Its value is correctness (auto-detect, security guards, frozen results, Result API), not throughput.
